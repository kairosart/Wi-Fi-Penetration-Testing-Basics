Click a *packet → expand*:

```nginx
IEEE 802.11 Wireless LAN
```

Look for:

- **Receiver address**
    
- **Transmitter address**
    
- **BSSID**
    
- **Signal strength (dBm)**
    
- **Channel**
    
- **SSID (inside beacon frames)**

